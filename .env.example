# Server
PORT=8787

# LLM routing:
# - mock: no external API calls, simple heuristics
# - openai: uses OpenAI-compatible API via OPENAI_API_KEY
LLM_PROVIDER=mock

# OpenAI (only needed when LLM_PROVIDER=openai)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# OPENAI_BASE_URL=http://localhost:11434/v1
